"use client";

import { Modal } from "@/components/ui/overlay";

// À IMPLÉMENTER (module Projets)
export function ProjectCreateModal({ onClose }: { onClose: () => void }) {
  return <Modal title="Nouveau projet" onClose={onClose}>…</Modal>;
}
