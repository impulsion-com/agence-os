"use client";

// À IMPLÉMENTER (module Calendrier/Timeline/Fichiers) : fichiers du projet (Supabase Storage).
export function FilesView({ projectId }: { projectId: string }) {
  return <div className="page faint">Fichiers {projectId}</div>;
}
