import { PreferencesSettings } from "@/components/workspace/settings/preferences";

export const metadata = { title: "Préférences" };

export default function PreferencesPage() {
  return <PreferencesSettings />;
}
